import { SelectedHotelPipe } from './selected-hotel.pipe';

describe('SelectedHotelPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedHotelPipe();
    expect(pipe).toBeTruthy();
  });
});
